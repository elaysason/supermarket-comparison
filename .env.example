# Preferred for production deployments:
DATABASE_URL=postgresql://api_readonly:your_password@db.your-project-ref.supabase.co:5432/postgres?sslmode=require
ALLOWED_EXTENSION_ORIGINS=chrome-extension://lpanbbdfjojpggjjigbeohneelcmheln
MAX_COMPARE_BARCODES=100
MAX_BARCODE_LENGTH=64
MAX_ITEM_QUANTITY=99
DATABASE_POOL_MIN_SIZE=1
DATABASE_POOL_MAX_SIZE=3
# API only; avoid this for scraper bulk loads.
# DATABASE_STATEMENT_TIMEOUT_MS=10000

# Backward-compatible local format:
user=postgres
password=your_supabase_password
host=db.your-project-ref.supabase.co
port=5432
dbname=postgres
